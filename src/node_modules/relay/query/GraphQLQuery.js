export default class GraphQLQuery {
  constructor(query) {
    this._query = query
  }

  isExecutable() {
    return /^\s*(query|\{)\s+/.test(this._query)
  }

  toString() {
    return this._query
  }
}
